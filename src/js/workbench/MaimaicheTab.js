import React, { Component } from "react";
import { View } from "react-native";

export default class MaimaicheTab extends Component {
  static navigationOptions = ({ navigation }) => {
    return {
      title: "买买车"
    };
  };
  render() {
    return <View />;
  }
}
